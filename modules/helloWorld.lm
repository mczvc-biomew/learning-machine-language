fun hello() {
    var hi = "Hello";
    var welcome = "welcome";
    return {
        world: " World!",
        greetings: || hi + this.world,
        greetings2: || "Hello! Greetings and " + welcome,
        greetings3: || this.greetings2() + "!"
    };
}
